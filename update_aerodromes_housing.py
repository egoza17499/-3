import psycopg2
import logging
from config import DATABASE_URL

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
logger = logging.getLogger(__name__)

# ============================================================================
# АЭРОДРОМЫ КОТОРЫЕ ОБЕСПЕЧИВАЮТСЯ ЖИЛЬЕМ МО РФ
# ============================================================================
AERODROMES_PROVIDED = {
    # ЦВО - обеспечиваются
    'ЦВО': [
        'Балашиха', 'Белая', 'Бишкек', 'Челябинск', 'Душанбе', 'Екатеринбург',
        'Новосибирск', 'Оренбург', 'Приозерск', 'Саратов',
    ],
    # ЮВО - обеспечиваются
    'ЮВО': [
        'Астрахань', 'Армавир', 'Ахтубинск', 'Бельбек', 'Гвардейское',
        'Миллерово', 'Ростов-на-Дону', 'Саки', 'Туапсе',
    ],
    # ЗВО - обеспечиваются
    'ЗВО': [
        'Воронеж', 'Иваново', 'Калининград', 'Кубинка', 'Липецк',
        'Псков', 'Пушкин', 'Рязань', 'Тверь', 'Тамбов', 'Торжок',
        'Шайковка', 'Саваслейка', 'Москва',  # Чкаловский - обеспечивается!
    ],
    # ВВО - обеспечиваются
    'ВВО': [
        'Владивосток', 'Домна', 'Комсомольск-на-Амуре', 'Николаевка',
        'Петропавловск-Камчатский', 'Хабаровск', 'Украинка', 'Улан-Удэ',
    ],
    # СФ - обеспечиваются
    'СФ': [
        'Вологда', 'Воркута', 'Мончегорск', 'Оленегорск', 'Североморск',
    ],
}

# ============================================================================
# АЭРОДРОМЫ КОТОРЫЕ НЕ ОБЕСПЕЧИВАЮТСЯ ЖИЛЬЕМ МО РФ
# ============================================================================
AERODROMES_NOT_PROVIDED = {
    # ЦВО - не обеспечиваются
    'ЦВО': [
        'Барнаул', 'Бердск', 'Братск', 'Йошкар-Ола', 'Каменск-Уральский',
        'Канск', 'Нижний Тагил', 'Омск', 'Пермь', 'Пугачев', 'Ртищево',
        'Самара', 'Сызрань', 'Татищево', 'Тоцкое', 'Ужур', 'Ульяновск',
        'Упрун', 'Чебеньки', 'Ясный',
    ],
    # ЮВО - не обеспечиваются
    'ЮВО': [
        'Анапа', 'Ашулук', 'Буденновск', 'Волгоград', 'Грозный', 'Ейск',
        'Зерноград', 'Капустин Яр', 'Кача', 'Кореновск', 'Котельниково',
        'Краснодар', 'Кушевская', 'Крымск', 'Майкоп', 'Моздок',
        'Морозовск', 'Приморско-Ахтарск', 'Таганрог', 'Тихорецк',
    ],
    # ЗВО - не обеспечиваются
    'ЗВО': [
        'Борисоглебск', 'Бутурлиновка', 'Владимир', 'Вязьма', 'Выборг',
        'Громово', 'Каш', 'Курск', 'Левашово', 'Мичуринск', 'Остафьево',
        'Плесецк', 'Ступино', 'Тула', 'Хотилово', 'Шаталово', 'Черняховск',
        'Петрозаводск', 'Ржев',
    ],
    # ВВО - не обеспечиваются
    'ВВО': [
        'Итуруп', 'Магдагачи', 'Могоча', 'Оловянная', 'Переяславка',
        'Среднебелая', 'Советская Гавань', 'Смирных', 'Темп', 'Тикси',
        'Хороль', 'Хурба', 'Черниговка', 'Чиндант', 'Чита', 'Чугуевка',
    ],
    # СФ - не обеспечиваются
    'СФ': [
        'Нагурская', 'Нарьян-Мар', 'Рогачево', 'Архангельск',
    ],
}

# ============================================================================
# ПРЕДЕЛЬНЫЕ СУММЫ ДЛЯ ГОРОДОВ (Приказ №600 от 31.08.2025)
# ============================================================================
CITY_LIMITS = {
    # Москва и Санкт-Петербург - 4500 руб
    'Москва': 4500,
    'Санкт-Петербург': 4500,
    
    # Города-миллионеры - 4000 руб
    'Новосибирск': 4000,
    'Екатеринбург': 4000,
    'Казань': 4000,
    'Нижний Новгород': 4000,
    'Челябинск': 4000,
    'Самара': 4000,
    'Омск': 4000,
    'Ростов-на-Дону': 4000,
    'Уфа': 4000,
    'Красноярск': 4000,
    'Пермь': 4000,
    'Воронеж': 4000,
    'Волгоград': 4000,
}

DEFAULT_LIMIT_PROVIDED = 3500      # Для обеспечиваемых (по умолчанию)
DEFAULT_LIMIT_NOT_PROVIDED = 3000  # Для не обеспечиваемых (по умолчанию)

def get_limit_for_city(city_name, is_provided):
    """Определить лимит для города"""
    if is_provided:
        return CITY_LIMITS.get(city_name, DEFAULT_LIMIT_PROVIDED)
    else:
        return CITY_LIMITS.get(city_name, DEFAULT_LIMIT_NOT_PROVIDED)

def update_aerodrome_housing(cursor, city, is_provided, district):
    """Обновить информацию о жилье для конкретного аэродрома"""
    
    limit = get_limit_for_city(city, is_provided)
    
    if is_provided:
        housing_text = f"Обеспечивается бесплатным жильем по линии МО РФ (максимальный лимит: {limit} руб/сутки)"
    else:
        housing_text = f"Не обеспечивается жильем МО РФ (компенсация гостиницы: {limit} руб/сутки)"
    
    # Обновляем по названию города
    cursor.execute("""
        UPDATE aerodromes 
        SET housing_info = %s,
            updated_at = NOW()
        WHERE (city = %s OR airport_name ILIKE %s OR name ILIKE %s)
        RETURNING id, city, airport_name
    """, (housing_text, city, f'%{city}%', f'%{city}%'))
    
    updated = cursor.fetchall()
    if updated:
        logger.info(f"✅ {city} ({district}) -> {housing_text[:60]}...")
        return len(updated)
    else:
        logger.debug(f"⚠️ Не найдено: {city} ({district})")
        return 0

def update_all_aerodromes():
    """Обновить все аэродромы"""
    
    logger.info("🚀 Начинаю обновление информации о жилье для аэродромов...")
    logger.info("📋 На основе Приказа №600 от 31.08.2025")
    logger.info("="*70)
    
    try:
        conn = psycopg2.connect(DATABASE_URL)
        cursor = conn.cursor()
        
        total_updated = 0
        stats = {'provided': 0, 'not_provided': 0}
        
        # Обновляем аэродромы с предоставлением жилья
        logger.info("\n📍 ОБНОВЛЯЕМ АЭРОДРОМЫ С ОБЕСПЕЧЕНИЕМ ЖИЛЬЯ:")
        logger.info("-"*70)
        for district, cities in AERODROMES_PROVIDED.items():
            logger.info(f"\n  {district} ({len(cities)} аэродромов):")
            for city in cities:
                updated = update_aerodrome_housing(
                    cursor, city, 
                    is_provided=True, 
                    district=district
                )
                total_updated += updated
                stats['provided'] += updated
        
        # Обновляем аэродромы без предоставления жилья
        logger.info("\n\n📍 ОБНОВЛЯЕМ АЭРОДРОМЫ БЕЗ ОБЕСПЕЧЕНИЯ ЖИЛЬЯ:")
        logger.info("-"*70)
        for district, cities in AERODROMES_NOT_PROVIDED.items():
            logger.info(f"\n  {district} ({len(cities)} аэродромов):")
            for city in cities:
                updated = update_aerodrome_housing(
                    cursor, city,
                    is_provided=False,
                    district=district
                )
                total_updated += updated
                stats['not_provided'] += updated
        
        # Сохраняем изменения
        conn.commit()
        
        # Финальная статистика из БД
        cursor.execute("""
            SELECT 
                COUNT(*) as total,
                COUNT(CASE WHEN housing_info LIKE '%Обеспечивается%' THEN 1 END) as provided,
                COUNT(CASE WHEN housing_info LIKE '%Не обеспечивается%' THEN 1 END) as not_provided
            FROM aerodromes
            WHERE housing_info IS NOT NULL
        """)
        
        db_stats = cursor.fetchone()
        
        # Вывод итогов
        logger.info("\n" + "="*70)
        logger.info("✅ ОБНОВЛЕНИЕ ЗАВЕРШЕНО УСПЕШНО!")
        logger.info("="*70)
        logger.info(f"📊 Всего обновлено записей: {total_updated}")
        logger.info(f"📊 С обеспечением жилья: {stats['provided']}")
        logger.info(f"📊 Без обеспечения жилья: {stats['not_provided']}")
        logger.info("\n📊 В БАЗЕ ДАННЫХ:")
        logger.info(f"   Всего с информацией: {db_stats[0]}")
        logger.info(f"   Обеспечивается: {db_stats[1]}")
        logger.info(f"   Не обеспечивается: {db_stats[2]}")
        logger.info("="*70)
        
        # Примеры
        logger.info("\n📝 ПРИМЕРЫ ОБНОВЛЕННЫХ ЗАПИСЕЙ:")
        logger.info("-"*70)
        cursor.execute("""
            SELECT city, housing_info 
            FROM aerodromes 
            WHERE housing_info IS NOT NULL 
            LIMIT 5
        """)
        examples = cursor.fetchall()
        for city, info in examples:
            logger.info(f"   {city}: {info[:70]}...")
        
        cursor.close()
        conn.close()
        
        return total_updated
        
    except Exception as e:
        logger.error(f"❌ Ошибка при обновлении: {e}")
        raise

def show_summary():
    """Показать сводку перед обновлением"""
    
    logger.info("\n" + "="*70)
    logger.info("ИНФОРМАЦИЯ ОБ ОБНОВЛЕНИИ")
    logger.info("="*70)
    logger.info(f"\n📋 Всего аэродромов для обновления:")
    
    total_provided = sum(len(cities) for cities in AERODROMES_PROVIDED.values())
    total_not_provided = sum(len(cities) for cities in AERODROMES_NOT_PROVIDED.values())
    
    logger.info(f"   ✅ С обеспечением жилья: {total_provided}")
    logger.info(f"   ❌ Без обеспечения жилья: {total_not_provided}")
    logger.info(f"   📊 ИТОГО: {total_provided + total_not_provided}")
    
    logger.info("\n💰 ПРЕДЕЛЬНЫЕ СУММЫ:")
    logger.info("   • Москва, Санкт-Петербург: 4500 руб/сутки")
    logger.info("   • Города от 1 млн чел: 4000 руб/сутки")
    logger.info("   • Столицы республик/областей: 3500 руб/сутки")
    logger.info("   • Остальные: 3000 руб/сутки")
    logger.info("="*70)

if __name__ == "__main__":
    logger.info("\n" + "="*70)
    logger.info("ОБНОВЛЕНИЕ ИНФОРМАЦИИ О ЖИЛЬЕ ДЛЯ АЭРОДРОМОВ")
    logger.info("На основе Приказа №600 от 31.08.2025")
    logger.info("="*70)
    
    show_summary()
    
    try:
        updated_count = update_all_aerodromes()
        logger.info(f"\n✅ Успешно обновлено {updated_count} аэродромов!")
        logger.info("\n💡 Проверьте информацию в боте:")
        logger.info("   Напишите: 'Москва' или 'Новосибирск'")
    except Exception as e:
        logger.error(f"\n❌ Ошибка: {e}")
